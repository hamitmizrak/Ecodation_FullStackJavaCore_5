package com.hamitmizrak;

public class _07_3_Conditional {

    // User
    static void main() {
        int number = +15;

        String result= (number > 0) ? number + " sayısı pozitif" : number + " sayısı negatif";
        System.out.println(result);

    }
}
